mixin char-card(data)
  .char-card
    .char-card__content
      h3.lw-text-md= data.title
      p.lw-text-sm.lw-fw-light= `Стоимость от: ${data.cost} ₽`

    a(href="#" data-modal-anchor= data.modalAnchor ? data.modalAnchor : null).char-card__btn= data.buttonText
